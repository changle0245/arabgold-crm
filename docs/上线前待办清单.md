# 上线前待办清单

> 来源:2026-05-18 满载压测后形成的修复清单。本清单收录的所有问题必须全部修复后才上线。
> **2026-05-27 进度审计**:24 项条目中 20 项已完成、4 项剩余,详见下方「进度总览」表。**P0/P1 全清,剩余 P2-⑫⑮ 小瑕疵 + P1-⑨ 层 2 增强(原文已建议留观察)+ P1-⑪ 业务定义问题**。
> "业务员能给别人指派提醒"——确认是正常设计,不在清单内。
>
> 文档格式说明:**改动大小** = 估算工时 / 影响半径,小=1-2h、中=半天-1天、大=1天以上。
> **不可逆** = 是否涉及 schema 不可回退的改动或会造成数据丢失。

---

## 进度总览 · 2026-05-27 审计

中台 Claude 全量代码审计 + 通过 commit / 代码注释("修 #x") / README 备注痕迹确认。原清单 2026-05-18 创建后未回填进度,实际改造方一直在修。

### ✅ 已完成 (20/24)

| 项 | 完成证据 |
|---|---|
| P0-1 三大列表分页 | customers / quotations / deals / reminders 各页 PAGE_SIZE=30 + `.range()` + `<Pagination>` 已实现 |
| P0-2 Turbopack 404 验证 | [`README.md:6`](../README.md) 已实测确认 dev only,`npm run build` 不复现(2026-05-25) |
| P0-3 业绩目标设置 | [`boss/page.tsx:46-85,372-462`](../src/app/(app)/dashboard/boss/page.tsx) 完整 modal + [`/api/system-settings`](../src/app/api/system-settings/route.ts) |
| P0-4 集中度风险 UI + 阈值设置 | [`boss/page.tsx:777-809`](../src/app/(app)/dashboard/boss/page.tsx) 永远渲染 + 空态"经营健康" + 阈值修改按钮 |
| P0-5 删客户 ConfirmModal | [`customers/[id]/page.tsx:484-520`](../src/app/(app)/customers/[id]/page.tsx) dangerLevel="high" + 输入客户名 + 8 张表级联枚举(实时计数) |
| P1-① MoM 月内进度修正 | [`boss/page.tsx:492-509`](../src/app/(app)/dashboard/boss/page.tsx) "本月已过 N/N 天,按当前进度预估月底约 X"(L12 修) |
| P1-② 漏斗 5 主阶段 + 沉默流失分支 | [`boss/page.tsx:321-340`](../src/app/(app)/dashboard/boss/page.tsx) `FUNNEL_MAIN_STAGES` 5 阶段 + `silentLossCount` 独立显示(修 #2) |
| P1-③ 业务员排行 selector | [`boss/page.tsx:42,665-674`](../src/app/(app)/dashboard/boss/page.tsx) `rankingScope = 'month'\|'quarter'\|'year'\|'all'` |
| P1-④ YAxis width | [`boss/page.tsx:654`](../src/app/(app)/dashboard/boss/page.tsx) `width={80}` |
| P1-⑤ 集中度阈值 | 合并到 P0-4(原清单已注) |
| P1-⑥ 无权限文案 | [`customers/[id]/page.tsx:265`](../src/app/(app)/customers/[id]/page.tsx) "客户不存在或您无权限查看" |
| P1-⑦ owner_changes 表 + timeline | `customer_ownership_changes` 表(migration `20260518020000`) + [`customer-form.tsx:216-221`](../src/components/customer-form.tsx) 写入 + [`customers/[id]/page.tsx:320-327`](../src/app/(app)/customers/[id]/page.tsx) timeline 渲染 |
| P1-⑧ 重置密码 API + UI | [`/api/members/[id]/password/route.ts`](../src/app/api/members/[id]/password/route.ts) + [`members/page.tsx:163-205,270-274`](../src/app/(app)/members/page.tsx) 表格按钮 + modal |
| P1-⑩ 强制首次改密 | `profiles.must_change_password` 列(migration `20260518030000`)+ members 创建时置 true + [`/account/change-password`](../src/app/account/change-password/page.tsx) 流程 |
| P1-⑬ 删除规则 tooltip | 3 panel 删除按钮都有 `title` 说明规则([`quotation:534`](../src/components/quotation-panel.tsx) / [`deal:473`](../src/components/deal-panel.tsx) / [`sample:236`](../src/components/sample-panel.tsx)) |
| **P1-⑭ window.confirm → ConfirmModal** | **commit `67bc182`(2026-05-27)** 替换 4 处:[报价](../src/components/quotation-panel.tsx) / [成交](../src/components/deal-panel.tsx) / [样品](../src/components/sample-panel.tsx) / [附件](../src/app/(app)/customers/[id]/page.tsx)。删客户在前批次已用 ConfirmModal |
| P1-⑨ 层 1 离职文案 | [`members/page.tsx:245`](../src/app/(app)/members/page.tsx) "取消勾选 = 离职,账号停用但数据保留" |
| P2-⑯ sidebar loading | [`sidebar.tsx:72-74`](../src/components/sidebar.tsx) profile loading 不 fallback 'member'(修 #⑯) |
| P2-⑰ Promise.all | [`customer-form.tsx:215-228`](../src/components/customer-form.tsx) sideEffects 并行(修 #⑰) |
| P2-⑱ 提前 1 天按钮 | [`reminders/page.tsx:188-190`](../src/app/(app)/reminders/page.tsx) + [`reminder-panel.tsx:172-175`](../src/components/reminder-panel.tsx) `Rewind` 按钮 |
| P2-⑲ dev 编译 25s | [`README.md:5`](../README.md) "Turbopack 冷启动正常现象" |

### ❌ 剩余 (4/24)

| 项 | 性质 | 备注 |
|---|---|---|
| **P2-⑫** 催款触发器 / seed payment | 技术 | `auto_reminder_after_deal` 触发器需加 INSERT payment 行(详见原文 P2-⑫);或 seed 脚本补 payment 类型。**改 trigger 涉及 migration,需 staging 验证**。~1h |
| **P2-⑮** auto_reminder_after_deal 去重 | 技术 | 触发器加 `exists(...)` 去重(同上,涉及 migration)。~1h |
| **P1-⑨ 层 2** 成员真删 | 业务+技术 | **原文已建议"层 2 留观察"**(实际工作流里离职软删才是常态)。若决定做需 ~4h(转单流程 + admin.deleteUser) |
| **P1-⑪** 阶段跳样品确认 | 业务 | **需先与乐哥确认业务定义**(强制"先寄样后成交" vs 允许跳)。默认"允许跳"则只需 README 备注;若"提示"需 deal-panel 加确认弹窗 |

### 部 prod 建议

- **若以"严格清空全部清单"为标准**:还差 4 项(2 项需业务决策、2 项需 staging 验证 migration),实际剩 ~2-3h 技术工作 + 2 个业务决策点
- **若以"P0/P1 全清 + P2 留观察"为标准**:**当前即可部 prod**
  - 理由:P2-⑫⑮ 是小瑕疵不阻塞核心流程(催款仍可手工建,重复提醒可手工取消);P1-⑨ 层 2 是可选增强;P1-⑪ 是业务边界问题不影响功能完整性
  - 建议:**先部 prod → 同时跟改造方对齐 P1-⑪ 业务定义 + 决定 P1-⑨ 层 2 是否要做**

---

## P0 致命(5 项,会阻止上线)

---

### P0-1 三大列表全无分页

**问题描述**
客户(100)、报价(108)、成交(56)、提醒(202)四个核心列表全部一次性 select + 一次性渲染所有 DOM。数据翻 5 倍会明显卡顿,翻 10 倍可能 OOM 或浏览器无响应。

**涉及文件**
- `src/app/(app)/customers/page.tsx:36-59`(`load()` 无 `.range()`),`:79-102`(前端 `useMemo` 全量 filter),`:192-280`(无分页地渲染全部行 + 移动端卡片)
- `src/app/(app)/quotations/page.tsx:29-51`,`:64-77`,`:182-244`
- `src/app/(app)/deals/page.tsx:32-54`,`:67-83`,`:198-269`
- `src/app/(app)/reminders/page.tsx:22-43`,`:111-164`

**修复方式**
1. 服务端分页:统一加 `.range(start, end)`,page size 20-50,后端取计数走 `{ count: 'exact', head: true }`。
2. 前端加分页组件:页码 + "上一页/下一页"。
3. 过滤+搜索改成发回数据库做(用 `.ilike` + `.in` 等),不要在前端全量 filter——否则换页后过滤错位。
4. 移动端卡片视图同步分页。

**改动大小:中**(4 个列表 + 1 个共用分页组件)
**DB 改动:无**
**不可逆:无**
**关联:** 与 ⑮ 提醒重复无直接关联;但与 P0-5 共用 confirm 组件无冲突。

---

### P0-2 `/customers/[id]/edit` 路由 dev 模式被 Turbopack 吞掉

**问题描述**
文件存在、`export default` 正常,但 dev 模式下首次访问直接 404,清 `.next` 重启后恢复。Turbopack 增量编译丢识别。生产构建(`npm run build && npm start`)是否受影响**未验证**。

**涉及文件**
- `src/app/(app)/customers/[id]/edit/page.tsx`(代码本身无问题)
- 实际故障在 `next` / Turbopack 缓存层

**修复方式**
1. 先跑 `npm run build && npm start`,在生产构建下完整走一遍创建→编辑→保存,确认生产构建可见、可用。
2. 若生产构建无问题:在 README/AGENTS.md 写一条"dev 偶发 404,清 `.next` 重启"备忘,**不阻塞上线**。
3. 若生产构建也复现:这是真实 bug,需要排查路由分段命名(可能因 `[id]` 和 layout 嵌套引发);可考虑提 next.js issue 或换 webpack 编译器。

**改动大小:小**(主要是验证 + 文档)
**DB 改动:无**
**不可逆:无**
**关联:** 独立。

---

### P0-3 业绩目标功能半成品(无任何设置入口)

**问题描述**
- `boss/page.tsx:209-271` 完整读取 `system_settings.monthly_revenue_target` 并展示"本月业绩目标"卡片;
- `system_settings` 表存在(`20260516050000` 迁移)、默认 `null`、RLS 限定 admin 才能 update;
- **完全没有前端设置入口**:0 按钮、0 modal、0 API 路由。admin 只能 SQL 直改数据库。

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:46-69`(读取),`:209-271`(展示)
- `src/app/api/` 下**缺** `system-settings/route.ts`
- 触摸:`supabase/migrations/20260516050000_monthly_revenue_target.sql`(种入默认)

**修复方式**
1. 加 API:`src/app/api/system-settings/route.ts`,支持 `GET /api/system-settings?key=X` 和 `PATCH /api/system-settings`(body: `{ key, value }`),用 service role 走服务端写入(避免暴露 RLS 细节)。
2. `boss/page.tsx` 业绩目标卡片右上角加"设置"按钮,弹 modal 输入金额,调 PATCH。
3. (可选)`/members` 旁加一个 admin 设置页 `/settings`,统一管所有 `system_settings`(留扩展)。

**改动大小:小-中**(1 API + 1 modal,~半天)
**DB 改动:无**(system_settings 表和默认行已就绪)
**不可逆:无**
**关联:** 与 **P0-4 集中度风险阈值** 共用同一套 system_settings UI。建议**一起做**,出一个通用"设置项 modal"。

---

### P0-4 集中度风险模块"幽灵化"

**问题描述**
`boss/page.tsx:472-505` 用 `{concentrationRiskCustomers.length > 0 && ...}` 包了整个模块;阈值默认 30%(`system_settings.concentration_risk_threshold`),seed 数据最大单客户占比 6%,RPC `get_concentration_risk_customers()` 永返空数组,用户**根本看不见模块存在**,以为系统没这个功能。

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:472-505`
- `supabase/migrations/20260516030000_concentration_risk_warning.sql`(默认阈值 0.30、RPC、RLS)

**修复方式**
**两个修复要同时做:**
1. **改默认阈值** 30% → 10%(或 8%),通过迁移更新 system_settings 的默认值。已存在的 row 不会受 default 影响,所以**需要显式 UPDATE**:
   ```sql
   UPDATE public.system_settings SET value = '0.10'::jsonb WHERE key = 'concentration_risk_threshold';
   ```
2. **模块永远显示**:把 `{concentrationRiskCustomers.length > 0 && ...}` 改成永远渲染,空时显示 "暂无超过阈值(X%)的客户,经营健康"。这样用户知道模块在,知道阈值是多少。
3. 在阈值卡片旁加"设置"按钮 + modal,admin 可以调整(8-30% 区间)。

**改动大小:小**
**DB 改动:有**(改 system_settings 的 value;建议出 migration `20260518XXXXXX_lower_concentration_threshold.sql`)
**不可逆:**没有 — 改默认值可后悔重改。
**关联:** **强关联 P0-3**(共用 system_settings 设置 UI)。**强关联 P1 ⑤**(实际就是同一件事,⑤ 在 P1 里又重复提了一次,清单合并到此项)。

---

### P0-5 删客户确认文案严重误导 + 用 window.confirm 无二次防护

**问题描述**
文案说"所有关联的联系记录和附件都将被删除",但**实际级联硬删**(`on delete cascade`):
- contact_logs、customer_attachments、customer_tags、stage_changes、reminders、quotations(连带 quotation_items)、deals(连带 deal_items)、samples

且用浏览器原生 `window.confirm()` 弹窗,在 Mac/Win/Edge 上样式不一,文字小到看不清后果。

**涉及文件**
- `src/app/(app)/customers/[id]/page.tsx:202-207`(handleDeleteCustomer + confirm 文案)
- 触摸所有 `on delete cascade` 表(级联行为本身不改)

**修复方式**
1. 改文案:列全级联表 + 当前客户名下的实际数据量,例:
   > 确认删除客户 **Saeed Al-Rashid**?
   > 这将同时永久删除:
   > - 联系记录 5 条
   > - 报价 1 个
   > - 成交 4 单(总额 USD 751,073)
   > - 样品 0 个
   > - 提醒 5 条
   > - 附件 0 个
   > - 阶段变更审计 2 条
   >
   > 此操作不可撤销。
2. 用自定义 Confirm Modal(配合 ⑭ 全局替换 window.confirm,做一个),要求用户**输入客户姓名**作为二次确认(高破坏性操作的常见模式)。
3. 删除前先发一个 `count` 查询拿到每张表的关联数,展示给用户。

**改动大小:中**(modal 组件 + 计数查询)
**DB 改动:无**
**不可逆:**操作本身就是不可逆的硬删,**这是修复的目标——加阻力**
**关联:** **强关联 ⑭**(写一个通用 Confirm Modal,所有 window.confirm 全替换),建议与 ⑭ 一起做。

---

## P1 严重(14 项)

### ① 环比 -86.6% 未做月内进度修正

**问题描述**
今天 5/18,本月才过了 17 天,有 2 单成交;上月满月有 16 单。直接对比得"环比 -86.6%",误导。

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:169-194`(MoM/YoY 计算)
- `:273-338`(展示)

**修复方式**
两种选择,任选其一即可:
1. **按日数比例修正**:`expectedThisMonth = lastMonth * (今天日数 / 上月总日数)`,对比展示用 `monthDealAmount vs expectedThisMonth`,标注"按 D/N 进度估算"。
2. **加注释**:卡片下方小字"本月已过 17 天,上月满月对比仅供参考"——成本最低但说服力一般。

**改动大小:小**
**DB 改动:无**
**关联:** 与 ③(都是本月数据撑不起的问题)。

---

### ② 漏斗百分比 100/75/50/25 语义不清 + 只显 4 阶段

**问题描述**
- `funnelData` 算的是**累计占比**(`sum(counts[i..end]) / sum(counts)`),但 UI 只显示百分比数字,用户会误读为"转化率";
- 只显 4 阶段(新接触/报价中/已寄样/已成交),`待定` 和 `沉默` 被排除——但沉默客户是经营信号,被砍掉信号丢失。

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:196-207`(算法),`:199`(funnelStages 写死 4 个),`:340-372`(展示),`:369-371`("不包含'沉默'和'待定'"声明)

**修复方式**
1. 阶段从 4 个扩到 6 个(把 待定 / 沉默 加进来),颜色用灰阶区分两端。
2. 百分比改语义,UI 加上"累计占比"或"转化率"明确字样;或把"100% 75% 50% 25%"改成"5 → 4 → 3 → 2(人)"绝对数 + 单独一行算总转化率。

**改动大小:小**
**DB 改动:无**

---

### ③ 本月业务员成交排行只 1 条数据撑不起

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:141-151`(memberDealRanking 算法,只取 monthDeals),`:420-438`(展示)

**修复方式**
默认改"近 3 个月",或卡片右上角加 selector:本月 / 近 3 月 / 近 12 月 / 当年。需要把 `monthDeals` 算法抽成可参数化的 helper。

**改动大小:小-中**
**DB 改动:无**
**关联:** 与 ①(都是本月数据撑不起)。

---

### ④ 各业务员客户数横向条形图 Y 轴标签拥挤

**涉及文件**
- `src/app/(app)/dashboard/boss/page.tsx:407-418`(BarChart, `YAxis width={60}`)

**修复方式**
`width={60}` → `width={80}` 或 `width={'auto'}`(recharts 支持)。3 字汉字够用,4 字以上需要再扩。

**改动大小:小**(1 行)
**DB 改动:无**

---

### ⑤ 集中度风险阈值 30% 不合理

**已合并到 P0-4。** 本项作为"压测发现的副产物",在 P0-4 一并处理(改默认阈值 + 加 UI 设置入口)。

---

### ⑥ 跨业务员访问别人客户 URL 文案"客户不存在"

**问题描述**
RLS 拦截后查不到客户,前端只能拿到 `null`,显示"客户不存在"。但客户其实存在,只是无权限。文案误导。

**涉及文件**
- `src/app/(app)/customers/[id]/page.tsx:210`

**修复方式**
改成 `"客户不存在或您无权限查看"`。1 行字。

**改动大小:小**(1 行)
**DB 改动:无**

---

### ⑦ 转单(业务员变更)不在客户事件时间线记录

**问题描述**
`customer-form.tsx:196-204` 编辑客户时,只有 `stage` 变化才 INSERT `stage_changes`;`owner_id` 改变(转单)**不写任何审计**。老板想审"这单当初是谁的"无法回溯。

**涉及文件**
- `src/components/customer-form.tsx:196-223`(handleSubmit)
- `src/app/(app)/customers/[id]/page.tsx:248-255`(timeline 渲染 stage_changes)
- 涉及 DB schema:`stage_changes` 表(可能改扩为通用 audit,或新建 `owner_changes` 表)

**修复方式(三选一)**
**A. 新建 `owner_changes` 表**(推荐,语义清晰):
- 字段:`id / customer_id / changed_by / from_owner_id / to_owner_id / changed_at`
- 在 customer-form 检测 `payload.owner_id !== customer.owner_id` 时 INSERT
- 详情页 timeline 加一种事件类型 `owner_change`,渲染"转单: 张磊 → 王芳"
**B. 改造 `stage_changes` 为 `customer_changes`**:加 `change_type` 列(`stage`/`owner`),from/to 改 text。改动面更大,要 backfill 历史数据。
**C. 用 generic audit log 表**:`audit_logs(entity_type, entity_id, action, before, after, by, at)`,扩展性强。

建议 **A**——最小改动,与现有 stage_changes 平行存在,前端 timeline 加 type 路由即可。

**改动大小:中**
**DB 改动:有**(新表 + RLS + migration)
**不可逆:**新表无影响。
**关联:** 详情页 timeline 渲染(`customers/[id]/page.tsx:248-255` 模式)需要扩。

---

### ⑧ 业务员忘密码,admin 无法重置

**问题描述**
`members/page.tsx` 没有"重置密码"按钮;`src/app/api/members/[id]/route.ts` 应该只有 PATCH,没 `/password` endpoint。admin 只能去 Supabase Studio 改。

**涉及文件**
- `src/app/(app)/members/page.tsx:131-203`(表格列,只有"编辑"按钮)
- `src/app/api/members/[id]/route.ts`(需要新增 POST 子路由或独立 password 路由)

**修复方式**
1. 加 API:`src/app/api/members/[id]/password/route.ts`,POST body `{ new_password }`,服务端用 `supabaseAdmin.auth.admin.updateUserById(id, { password })`。
2. members 表格加"重置密码"按钮,弹 modal 输入新密码 → 调 API → 提示业务员下次登录用新密码。

**改动大小:小-中**
**DB 改动:无**
**关联:** 与 ⑨⑩ 一起做,统一成员管理。

---

### ⑨ 不能删成员,只有"离职(is_active=false)"状态

**问题描述**
当前只能软删(把 is_active 置 false),但 UI 没有明示这是"软删"还是"硬删",用户期望可能是真删。

**涉及文件**
- `src/app/(app)/members/page.tsx:168-173`(在职 checkbox)

**修复方式**
两个层次:
1. **文案层(立刻可做)**:在编辑成员的"在职"checkbox 旁加说明"取消勾选 = 离职(账号停用但数据保留)"。
2. **真删能力(可选)**:加"彻底删除"按钮,但要先检查该成员名下是否还有客户:
   - 有客户 → 弹窗强制选"批量转单给 XX 业务员"再删
   - 无客户 → 二次确认后调 admin.deleteUser(id) + 删 profile

建议**先做层 1 文案,层 2 留观察**(实际工作流里"离职但保留账号"才是常态)。

**改动大小:小(层 1)/ 大(层 2)**
**DB 改动:无**
**不可逆:**层 2 是 — 硬删后历史成交人无法追溯;层 1 无影响。
**关联:** 与 ⑧⑩ 同会话做。

---

### ⑩ 初始密码不强制首次登录修改

**问题描述**
admin 创建成员时设的"初始密码"会一直能用。安全隐患:admin 知道每个成员的明文密码。

**涉及文件**
- `profiles` 表(需要加列 `must_change_password boolean default false`)
- 登录后中间件 / `(app)/layout.tsx` 检查
- 新增页面 `/account/change-password` 或 modal

**修复方式**
1. 迁移:profiles 加列 `must_change_password boolean default false not null`。
2. admin 创建新成员时,设 `must_change_password = true`。
3. AuthProvider / layout 检测到 `must_change_password = true` → 强制跳改密页,改完置 false。
4. 改密页本身:输入旧密 + 新密 + 确认,调 `supabase.auth.updateUser({ password })`。

**改动大小:中**
**DB 改动:有**(profiles 加 1 列 + migration)
**不可逆:**无(新列默认 false,不影响存量)
**关联:** 与 ⑧ 改密功能可复用 API。

---

### ⑪ 阶段流转跳过"已寄样"

**问题描述**
触发器 `advance_stage_on_deal` 的 guard 是 `v_old_stage <> '已成交'`,任何阶段 + deal = 直接跳已成交。业务员录 deal 时,如果客户还在"报价中",中间的"已寄样"被跳过(从语义上无法回溯客户是否寄过样)。

**涉及文件**
- `supabase/migrations/20260517010000_record_stage_changes_in_triggers.sql:96-123`(advance_stage_on_deal,guard 逻辑)
- 业务侧:`src/components/deal-panel.tsx`(录入入口)

**修复方式**
**取决于业务定义,需先与产品确认:**
- **如果"先寄样后成交"是强制路径**:deal-panel 在 stage 不在 `已寄样` 时弹确认"该客户尚未寄样,确认录入成交吗?";不改触发器。
- **如果跳样品成交合法**(展会直接下单等场景):**不改任何代码**,在产品手册里写明阶段流转规则。

建议**先与乐哥确认业务定义**,默认走"允许跳"。

**改动大小:小(选择允许跳)/ 小(加确认弹窗)**
**DB 改动:无**(触发器不动)

---

### ⑫ "催款"类型提醒 seed 0 条

**问题描述**
seed 数据没生成 `payment` 类型 reminder。生产环境本质是因为**没有触发器自动建催款提醒**,只能业务员手工建。

**涉及文件**
- 触发器侧:无对应自动生成路径(`auto_reminder_after_deal` 只建 follow_up)
- 手工建路径:`src/components/reminder-panel.tsx`(用户可选 type=payment)

**修复方式**
两条路径,**建议都做**:
1. **修复 seed**:不算 bug,在 `seed-test-data.js` 加一段,给若干 deals 的 deposit_received=false 客户手工建 1 条 payment 提醒(脚本里,不进生产)。
2. **加触发器**:`auto_reminder_after_deal` 同时建一条 payment(due_date = deal_date + 7d)的 reminder。或仅在 `deposit_received = false` 时建。

**改动大小:小(只补 seed)/ 中(加触发器)**
**DB 改动:**仅"加触发器"路径有,需要新 migration

---

### ⑬ 单条删除设计不一致

**问题描述**
- 报价(quotation):有 parent_id 版本链,必须"按版本反向删",有子版本会被挡(`quotation-panel.tsx:227`)
- 成交(deal):可直接删,删后客户 total_deal_count/amount 由 `refresh_customer_deal_stats` 触发器同步重算
- 样品(sample):可直接删

用户视角"删一个报价老被挡"= 体验上的"不可删";三种规则混在一起,无文档说明。

**涉及文件**
- `src/components/quotation-panel.tsx:220-239`(版本链锁)
- `src/components/deal-panel.tsx:240+`
- `src/components/sample-panel.tsx:126+`

**修复方式**
1. 文档说明:每个 panel 的删除按钮旁加 tooltip 解释规则(报价"先删新版本"/ 成交"会同步更新客户累计"/ 样品"直接删")。
2. (可选)报价:若 admin 有权,允许"递归删除整条版本链"——但要 P0-5 同款二次确认。

建议**只做 tooltip**,递归删暂不做。

**改动大小:小**
**DB 改动:无**

---

### ⑭ 删除用 window.confirm,与系统设计语言不一致 + 无防护

**涉及文件**
- `src/components/quotation-panel.tsx:231`
- `src/components/deal-panel.tsx:240`
- `src/components/sample-panel.tsx:126`
- `src/app/(app)/customers/[id]/page.tsx:196`(删附件),`:203`(删客户)

**修复方式**
写一个通用 `<ConfirmModal>` 组件:`src/components/confirm-modal.tsx`,支持 title / body / 危险等级 / 输入确认词功能(参考 GitHub 删 repo)。所有 `confirm()` 调用全替换。

**改动大小:中**(组件 + 5 个调用点替换)
**DB 改动:无**
**关联:** **强关联 P0-5**——P0-5 直接用这个 modal,且要求"输入客户名确认"功能。建议**先建 modal,再做 P0-5**。

---

## P2 小瑕疵(5 项)

### ⑮ 重复提醒生成:同客户 N 单成交 → N 条同样"成交后 30 天回访"

**问题描述**
触发器 `auto_reminder_after_deal` 在每个 deal INSERT 后建一条 follow_up reminder。同客户 4 单 → 4 条相同 reminder(已在 Saeed Al-Rashid 抽查中复现)。

**涉及文件**
- `supabase/migrations/20260517020000_localize_reminder_trigger_dates.sql:68-88`(auto_reminder_after_deal)

**修复方式**
触发器内增加去重判断:`exists(... where customer_id=new.customer_id and type='follow_up' and status='pending' and due_date BETWEEN X-1 AND X+1)` 则不插。
或:对同客户最近一条 pending follow_up 直接 UPDATE due_date 到新 deal 的 +30(business-wise 更合理:把回访日期顺延到最新成交后)。

**改动大小:小**
**DB 改动:有**(改触发器,出 migration)

---

### ⑯ 详情页 loading 时侧栏临时少 2 项,加载完恢复

**问题描述**
profile 加载中,`useAuth()` 返回 profile=null。sidebar 第 56-58 行 `profile?.role || 'member'` 兜底成 'member',admin-only 项(老板大屏/成员管理)被 filter 掉;profile 加载完成 → admin → 重新出现。视觉上闪一下。

**涉及文件**
- `src/components/sidebar.tsx:56-58`

**修复方式**
profile loading 时 return null(或骨架屏),不要用兜底 'member'。简单 1 行:`if (!profile) return <SkeletonNav />` 或直接 `return null`。

**改动大小:小**
**DB 改动:无**

---

### ⑰ /edit 保存 4s 偏慢——4 个 await 串行

**问题描述**
`customer-form.tsx:196-223` edit 路径:`update customers` → 可能 `insert stage_changes` → `delete customer_tags` → `insert customer_tags`。即使本地库 1 次 RPC ≈1s,4 次也 4s。

**涉及文件**
- `src/components/customer-form.tsx:165-223`(handleSubmit)

**修复方式**
- `syncTags` 和 `stage_changes` 互不依赖,改 `Promise.all([...])`。
- `delete + insert customer_tags` 在生产实际是"diff",但小数据集 delete+insert 也可接受;若想再快,做差量 INSERT/DELETE。
- 跳转 `router.push` 不用 await。

**改动大小:小**(改 1 函数,~30 分钟)
**DB 改动:无**

---

### ⑱ 提醒只有"推迟 1 天"没"提前 1 天",误推无法撤销

**涉及文件**
- `src/app/(app)/reminders/page.tsx:53-59`(postpone),`:146-149`(button)
- `src/components/reminder-panel.tsx:72-78`,`:167-170`

**修复方式**
两个文件都加 -1 天按钮:`<button onClick={() => postpone(r.id, -1)}>`,图标用 `<Clock>` 反向 / `<RotateCcw>` / 干脆改成日期 picker。

**改动大小:小**
**DB 改动:无**

---

### ⑲ dev 模式首次编译 ~25s

**非 bug**,Turbopack 初次冷启动正常。生产构建(`next build` 输出 .next/standalone 或 .next/static)与 dev 编译时间无关。

**修复方式**
**无需修复**,在 README/AGENTS.md 写一行"dev 首次启动需 20-30s,正常现象"。

**改动大小:小**(1 行文档)
**DB 改动:无**

---

## 关联矩阵(改 A 会牵动 B)

```
P0-3 业绩目标设置入口 ─┬─ 共用 system_settings API ─→ P0-4 集中度阈值设置
                       └─ 共用 settings modal ────────→ P0-4

P0-5 删客户改 modal ──── 共用 ConfirmModal 组件 ──→ ⑭ 全局替换 window.confirm
                                                     ├─→ quotation 删除
                                                     ├─→ deal 删除
                                                     └─→ sample 删除

⑦ 转单审计新表 ─────────── 详情页 timeline 渲染扩展 ──→ customers/[id]/page.tsx

⑧ 重置密码 API ─┬─ /api/members/[id]/password
                └────────── 公用 service-role auth admin SDK ─→ ⑩ 强制首次改密

⑤(并入 P0-4) ─── 同事项

①+③ 都是"本月数据撑不起" ── 改月份范围可选 selector 一起做

P0-1 列表分页 ────── 与所有列表页有关,但独立可做

⑪ 阶段跳样品 ────── 需先与乐哥确认业务定义,可能完全不动代码
```

---

## 建议施工顺序

按 **"先建公共组件 + DB schema,再做 P0,后做 P1/P2"** 的顺序。每一组内的项前置依赖已对齐。

### 第 1 组:公共基建(0.5-1 天)

> **必须先做** —— 后续多项都依赖。

| 顺序 | 项 | 工作 |
|---|---|---|
| 1.1 | ⑭ | 写 `<ConfirmModal>` 组件(支持文字确认输入、危险等级) |
| 1.2 | P0-3 + P0-4 公共 API | 写 `/api/system-settings` (GET/PATCH) |
| 1.3 | ⑦ | 写 migration:新建 `owner_changes` 表 + RLS |
| 1.4 | ⑩ | 写 migration:`profiles.must_change_password` 加列 |
| 1.5 | ⑮ | 写 migration:改 `auto_reminder_after_deal` 触发器加去重 |
| 1.6 | P0-4 默认值 | 写 migration:`UPDATE system_settings SET value='0.10' WHERE key='concentration_risk_threshold'` |

**结果**:DB 一次性升级到位,后续都在前端做。

---

### 第 2 组:P0 致命项(1-1.5 天)

| 顺序 | 项 | 工作 |
|---|---|---|
| 2.1 | P0-2 | 跑 `npm run build && npm start`,验证生产构建是否复现。若不复现 → README 记一笔关闭。若复现 → 排查 routing。 |
| 2.2 | P0-3 | boss/page 加"设置业绩目标"按钮 + modal,接 1.2 API。 |
| 2.3 | P0-4 | boss/page 把 `length > 0 &&` 拿掉,空态显示"经营健康";加"设置阈值"按钮 + modal,接 1.2 API。 |
| 2.4 | P0-5 | 用 1.1 ConfirmModal,改写删客户文案 + 列实际级联数据量 + 要求输入客户名确认。 |
| 2.5 | P0-1 | 4 个列表加 `.range()` 分页 + 共用分页组件,过滤改服务端。**这项最大,可以拆给一天专门做。** |

---

### 第 3 组:权限/审计/成员(1 天)

| 顺序 | 项 | 工作 |
|---|---|---|
| 3.1 | ⑦ | customer-form 加 owner_changes 写入;详情页 timeline 加 owner_change 事件类型。 |
| 3.2 | ⑥ | 详情页"客户不存在" → "客户不存在或您无权限查看"(1 行)。 |
| 3.3 | ⑧ | 加 `/api/members/[id]/password`,members 表格加"重置密码"按钮 + modal。 |
| 3.4 | ⑩ | AuthProvider/layout 检测 must_change_password,新建改密页;创建成员时默认置 true。 |
| 3.5 | ⑨ 层 1 | 在 is_active 切换旁加文案"取消勾选 = 离职(账号停用但数据保留)"。 |

---

### 第 4 组:业务流/老板大屏 P1(半天)

| 顺序 | 项 | 工作 |
|---|---|---|
| 4.1 | ⑪ | **先与乐哥确认业务定义**,如选"允许跳样品" → 仅 README 记;如选"提示" → deal-panel 加确认弹窗。 |
| 4.2 | ⑫ | seed 脚本补 payment 提醒(单独 PR/脚本);触发器是否自动建留观察。 |
| 4.3 | ⑬ | 报价/成交/样品的删除按钮加 tooltip 说明规则。 |
| 4.4 | ① | MoM/YoY 卡片加"本月已过 N 天"小字,或按日数比例修正。 |
| 4.5 | ② | 漏斗加 待定 / 沉默 两阶段,加"累计占比"明确字样。 |
| 4.6 | ③ | 本月业务员排行改"近 3 月" or 加 selector。 |
| 4.7 | ④ | BarChart YAxis width 60 → 80。 |

---

### 第 5 组:P2 小瑕疵(2-3 小时)

| 顺序 | 项 | 工作 |
|---|---|---|
| 5.1 | ⑯ | sidebar profile loading 时不兜底 'member',改 return null。 |
| 5.2 | ⑰ | customer-form syncTags + stage_changes 并行化,Promise.all。 |
| 5.3 | ⑱ | 两处 postpone 按钮加 -1 天反向按钮。 |
| 5.4 | ⑲ | README/AGENTS.md 加一行 dev 首次编译 25s 说明。 |
| 5.5 | ⑨ 层 2 | (可选)成员真删功能,如果需要做。 |

---

## 总览

- **新 migration 数:5**(owner_changes / must_change_password / auto_reminder_after_deal 去重 / concentration_threshold 默认 / 可能 ⑪⑫ 触发器)
- **新 API 路由:2**(system-settings / members password)
- **新组件:2**(ConfirmModal / 分页组件)
- **DB 改动条目:5**,**不可逆操作:0**(全是新增列/新表/改默认值,可回滚)

按上面顺序走,**~4-5 个工作日**应能全部完成 + 自测过线。
